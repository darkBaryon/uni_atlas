# Python
__pycache__/
*.pyc
.venv/
venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# macOS
.DS_Store

# 本地凭据（数据库密码在 ~/.my.cnf，永远不进仓）
*.cnf
.env

# 抓取的原始网页快照（体积大，存数据库或本地，不进仓）
snapshots/

# 试点期的一次性脚本与中间数据（已被 crawler/ 框架取代，仅本地留档）
crawler/legacy/

# 运行日志
logs/

# Playwright 浏览器会话配置（含 cookie/指纹，勿入仓）
.browser-profile/

# Claude Code 本地会话/配置（含 worktree 元数据，勿入仓）
.claude/
