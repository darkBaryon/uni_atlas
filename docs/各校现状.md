# 各校现状台账（英国 10 校）

> 最后对账：2026-07-05。数字会过期，**准数看 `./run.sh audit`**（逐校断言矩阵）；
> 本文管「数字背后的原因和决策」——为什么缺、卡在哪、拍板过什么。
> 策略背景见 [数据策略.md](数据策略.md)。

## 一览（2026-07-05 快照）

| 校 | 专业 | 归属 | 课程 | 链接 | 中文名 | 未来考试期 | 状态 |
|---|---|---|---|---|---|---|---|
| UCL | 990 | 100% | 5,484 | 98% | 100% | 0† | ✅ 收口 |
| 爱丁堡 | 654 | 100% | 5,538 | 100% | 100% | 0† | ✅ 收口 |
| 格拉 | 426 | 97% | 5,118 | 98% | 100% | 3 | ✅ 收口 |
| 曼大 | 685 | 98% | 4,820 | 90% | 100% | 3 | ✅ 收口 |
| 利兹 | 503 | 100% | 3,891 | 100% | 100% | 2 | ✅ 收口 |
| 布里斯托 | 421 | 100% | 0‡ | — | — | 17 | ✅ 收口（课程已拍板不抓） |
| 华威 | 444 | **46%** | 1,365 | 100% | 100% | 1 | 🔶 剩专业归属 |
| 伯明翰 | **14** | 64% | 抓取中 | 100% | 补中 | 0† | 🔄 进行中 |
| KCL | **45** | 91% | 0 | — | — | 4 | 🔄 等限流解冻 |
| 谢菲 | 269 | 100% | 47‡ | 0% | — | 0† | ⏸ 课程已拍板搁置 |

† = 非解析问题（见各校小节）　‡ = 结构性缺（已拍板）

## 逐校备注（原因与决策）

### UCL ✅
- 课程 5,484 来自专业页内联；目录总链接存 extra。
- **未来校历 0**：25/26 学年已结束，**26/27 官网未发布**（四重证据核实过），
  月度任务候发布，红灯属实不用修。
- 尾巴：3 个新专业页（官网 520 故障时发现，自动重试队列）；2 张
  js_render/pdf 目录页 v1 不抓。

### 爱丁堡 ✅
- 课程全在独立域名 **DRPS**（drps.ed.ac.uk），三层索引收 5,538 门。
- 考试期：25/26 有（已考完）；**26/27 官网标 "To be confirmed"**，候发布。
- 3 个 SSO 登录墙任务已封存（semester-dates 部分链接跳微软登录）。

### 格拉斯哥 ✅
- 课程 5,118：coursecatalogue browsebyschool 按学院收，自带归属。
- 归属 67%→97%：三层修复（反向索引 + faculty_overrides 人工层）。

### 曼彻斯特 ✅
- 课程 4,821：**uPortal portlet**（用户探明免登录路径）→ `portlet_post`
  抓取方式（先 GET render.uP 预热会话再 POST，参数全在任务 URL）。
- 课程归属经专业引用（portlet 无院系字段），expect 已注明。
- 尾巴：曼大分轮截止 round_no（30 条 NULL 轮次）。

### 利兹 ✅
- 课程 3,891：catalogue.leeds.ac.uk 全开放。三个坑都修了：TP 代码带字母
  尾缀（COMP5122M）、课名列位随页面变、区块标题是教学单元名（17 条 alias）。
- 考试期曾被错标 teaching_period（"Examinations" 分节头被子标签覆盖），
  已修，26/27 两窗口在库。

### 布里斯托 ✅（课程已拍板）
- 专业归属 37%→100%：权威源是专业页 `<meta name="faculty">`
  （历代措辞 17 alias + 8 slug overrides），全程离线重放修复。
- **课程目录被 FortiWeb WAF 硬挡**（JS 挑战走通仍 403，认浏览器指纹）。
  **2026-07-05 拍板：不啃**——前端 0 课程院系自动兜底「官方课程总目录 ↗」
  链接（真人浏览器不受影响）。Playwright 方案封存。
- 校历全场最佳（17 个未来考试期）。

### 华威 🔶
- **"429 封锁"是冤案的一半**：模块目录子域 courses.warwick.ac.uk 全开放
  （20 路并发全 200，就是慢）→ 高并发配置 2 分钟收 1,365 门、100% 归属。
  **主站 warwick.ac.uk 真限流**（termdates 中招过），保守档。
- 校历修复：termdates 快照一直有货是版式没吃到（标签行+日期行成对），
  52 事件 13 学年；考试期另有教务处页面（caption 定学年的表格），
  8 窗口/学年。
- **剩余：专业归属**——新模板专业页有 info-block 归属字段，但主站限流
  极严（12s/发也每几页一个 429），443 页重抓磨到 ~226/444，归属随入库
  爬升（当前 205）。**不值得为它引代理**：模块目录已 100%，归属是锦上
  添花，让它自己磨，磨不完不影响课程数据。Playwright 无用（限流非渲染
  问题，真浏览器一样被数）。系反向索引（/departments/academic/）已备为
  第二路。

### 伯明翰 ✅（2026-07-05 收口）
- 专业 14→698：营销层全量在 **/study/sitemap.xml**（702 URL，同 KCL 修法）。
- 归属 64%→**83%**：用手册（行政层）「学院→专业」索引反填营销层
  （前缀容忍模糊匹配，手册会截词 Sci/Com）。剩 121 个（82 PGT）是
  营销名与手册行政名重写差异过大无法可靠匹配——手册对部分学院只登记
  本科变体（Law 无 LLM，实测 UG/PG 入口同表），非抓漏。
- 跨源重复教训（已修进 loader）：专业页课表引用（无码）曾另建 3,600
  条骨架行稀释归属——现按名兜底匹配带码行，course↔program 引用 1.9 万条。
- 课程：手册三层（学校 52 → 专业 → 模块名单）已接入管道；servlet 是
  **会话态**（须先访问 schoolList+任一 programList，域级 prime 链在 yaml
  domains 配置）；002027 未来学年任务已降级。名单页 ~2,130 张抓取中。
- 旧 251 条无代码行（含 "Modules"/"Year 1" 垃圾）已下线，由手册名单取代。
- 考试期：公开学年页不公布考试窗口（仅提夏季补考），expect 注明。

### KCL ⏸（限流搁置）
- **专业缺 92%**（45/382）：A-Z 是 React SPA 静态抓不全；**编号 sitemap**
  才是权威源（sitemap1-8.xml，382 个 URL 已登记）。抓到 33 张时触发
  **Akamai 429**（起因：interval 1.0×3 并发过猛；已回 3.0×1）。
- **2026-07-05：惩罚窗口超 10 小时未解**——Akamai 对触发过的 IP 记账很久，
  正常速率也一律 429。**判断：搁置**，不引代理基础设施（KCL 课程本就只有
  交换生子集，价值有限）。续抓 349 张专业页 + 交换生模块的任务已在册，
  换网络出口（不同 IP）或过一两天窗口自然过后，`./run.sh --due --uni kcl`
  即自动接上——不阻塞其他工作。Playwright 无用（同华威，限流非渲染）。
- 课程：主课程页只有散文无结构；唯一公开源是**交换生模块目录**
  （~37 院系，带代码/学期/学分）——**子集非全量**，解析器和 9 条学部
  别名已备好，来源备注明确标注，排在专业页之后。
- 教训（已进 yaml 注释）：Akamai 的限流是报复性的，触发后惩罚窗口内
  连礼貌速率也一起封——宁可慢起步。

### 谢菲尔德 ⏸（课程已拍板搁置）
- 专业/校历良好（269 个、归属 100%）。
- **无公开中央课程目录**：模块数据散在 ~200 个专业规格 PDF
  （`/calendar/progspec/`，需 PDF 解析）或各院系自建页（格式不统一）；
  考试时间表在 MUSE 登录墙内。库里 47 门是专业页散文刮的（无码无链接）。
- **2026-07-05 拍板：先搁置**（同布里斯托逻辑，前端兜底提示）。
  将来谢菲辅导单量大再回头——可只啃有真实学生的院系，按需单点补。
- 原 extra 里的目录链接指向校历页（误导），已清除。

## 通用机制备忘

- **数据体检**：`./run.sh audit`——13 断言/校，红灯退出码 1，挂在
  `./run.sh` 日常流程末尾；每校差异走 yaml `expect:` 段（阈值/跳过+原因）。
- **验收三步法**：完整性 diff（防爬漏）→ 课程名单三问（防源头缺登记）→
  覆盖率+分布（防解析浅）。见 [数据策略.md](数据策略.md) 第〇节。
- **抓取方式**：html / portlet_post（uPortal 预热+POST）/ 域级 prime 链
  （会话态 servlet，yaml domains 配置）/ js_render+pdf（v1 不抓，预留）。
- **限流经验**：华威主站、KCL Akamai 均为报复性限流——高并发只给实测
  无限流的域（利兹、courses.warwick）；新域一律保守起步。
